#!/usr/bin/env bash
# zig/bin/test

source "$(dirname "$0")/_lib.sh"

banner

require_command zig
require_command curl

echo "Running unit tests..."
zig test howdy.zig

echo "Running request test..."
curl -sf http://localhost:3000 | grep "Howdy, World!"
