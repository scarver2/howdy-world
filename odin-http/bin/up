#!/usr/bin/env bash
# odin-http/bin/up

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

source "$SCRIPT_DIR/_lib.sh"

cd "$ROOT_DIR"

info "Starting odin-http container"

docker compose up --build --remove-orphans "$@"

