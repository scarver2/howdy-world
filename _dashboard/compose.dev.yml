# _dashboard/compose.dev.yml
services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: dashboard-dev
    ports:
      - "8080:8080"
    volumes:
      - ./www:/srv/www:ro
