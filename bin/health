#!/usr/bin/env bash
# bin/health

# ROOT_DIR="$(git rev-parse --show-toplevel)"

source "$(dirname "$0")/_lib.sh"

echo "Running Howdy World Health Check"
echo

run_all_contracts
contracts_exit_if_failed
