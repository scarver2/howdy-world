#!/usr/bin/env bash
# bin/restart

source "$(dirname "$0")/_lib.sh"

banner

"$SCRIPT_DIR/down"

# Pass arguments to up if present. Null arguments passed results in non-daemonized Docker Compose.
if [[ $# -eq 0 ]]; then
  "$SCRIPT_DIR/up"
else
  "$SCRIPT_DIR/up" "$@"
fi
