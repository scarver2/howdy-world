#!/usr/bin/env bash
# bin/restart

source "$(dirname "$0")/_lib.sh"

# SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# TODO: confirm no behavior changed before removing this section
# NO_BUILD=0
# if [[ "${1:-}" == "--no-build" ]]; then
#   NO_BUILD=1
#   shift
# fi
# compose down --remove-orphans

# if (( NO_BUILD == 1 )); then
#   if [[ $# -eq 0 ]]; then
#     compose up -d
#   else
#     compose up "$@"
#   fi
# else
#   if [[ $# -eq 0 ]]; then
#     compose up -d --build
#   else
#     compose up --build "$@"
#   fi
# fi

"$SCRIPT_DIR/down"
"$SCRIPT_DIR/up" "$@"
