#!/usr/bin/env bash
# python-fastapi/bin/restart

source "$(dirname "${BASH_SOURCE[0]}")/_lib.sh"

ROOT="$(howdy_root)"
cd "$ROOT"

howdy_require_brew_if_macos
howdy_require_docker

COMPOSE="$(howdy_compose)"
FILE="$(howdy_compose_file)"

howdy_echo "Restarting production services (compose.yml)"
$COMPOSE -f "$FILE" restart
