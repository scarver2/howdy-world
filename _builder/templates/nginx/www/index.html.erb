<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>Howdy World</title>
  <!-- TODO: move styles block to CSS file -->
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; margin: 40px; background: #f7f7f7; color: #222; }
    h1 { margin: 0 0 6px 0; }
    .sub { margin: 0 0 24px 0; color: #555; }
    .grid { display: grid; grid-template-columns: 1fr; gap: 18px; }
    .card { background: #fff; border-radius: 12px; padding: 16px 18px; box-shadow: 0 1px 3px rgba(0,0,0,.08); }
    .card h2 { margin: 0 0 10px 0; font-size: 18px; text-transform: capitalize; }
    ul { margin: 0; padding-left: 18px; }
    li { margin: 6px 0; }
    a { color: #0645AD; text-decoration: none; }
    a:hover { text-decoration: underline; }
    .meta { font-size: 12px; color: #666; margin-left: 8px; }
    .app-item { margin: 10px 0; }
    .badges { margin-left: 10px; display: inline-flex; flex-wrap: wrap; gap: 6px; vertical-align: middle; }
    .badge {
      font-size: 11px;
      line-height: 1;
      padding: 4px 8px;
      border-radius: 999px;
      background: #eee;
      color: #333;
      border: 1px solid #ddd;
      white-space: nowrap;
    }
    .badge-family { background: #eef6ff; border-color: #d7eaff; }
    .badge-category { background: #f6f1ff; border-color: #eadfff; }
    .badge-service { background: #effbf3; border-color: #d7f3e2; }
    .badge-runtime { background: #fff7e6; border-color: #ffe3ad; }
    .badge-active { background: #f3f3f3; border-color: #dedede; }
  </style>
</head>
<body>
  <h1>Howdy World</h1>
  <p class="sub">Choose an endpoint. Generated from CSV.</p>

  <div class="grid">
    <% grouped.each do |category, items| %>
      <div class="card">
        <h2><%= category %></h2>
        <ul>
          <% items.each do |svc| %>
            <li class="app-item">
              <a href="/<%= svc[:slug] %>/"><%= svc[:display_name] %></a>
              <span class="badges"><%= HowdyBuilder::Badges.html_badges(svc) %></span>
              <% if svc[:url].present? %>
                <span class="meta">· <a href="<%= svc[:url] %>" target="_blank" rel="noopener">Learn more</a></span>
              <% end %>
            </li>
          <% end %>
        </ul>
      </div>
    <% end %>
  </div>
</body>
</html>
