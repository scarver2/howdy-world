# elixir-hologram/compose.yml
services:
  elixir-hologram:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      HOLOGRAM_START: "1"
      MIX_ENV: dev
      PORT: "4000"
    ports:
      - "4000:4000"
